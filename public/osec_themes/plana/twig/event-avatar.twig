<div class="{{ classes }}">
    {% if link %}
        <a href="{{ link }}">
    {% endif %}
            <img itemprop="image" src="{{ src }}" alt="{{ alt }}" {% if width %} width="{{ width }}"{% endif %} {% if height %} height="{{ height }}"{% endif %} />
    {% if link %}
        </a>
    {% endif %}
</div>
